# Variants

- /variants/a : keep drawers, simplify topbar, remove query tip
- /variants/b : no topbar actions except hamburger + plus, panel button; denser blocks
- /variants/c : ultra-minimal (Logseq-ish): almost full-screen editor, actions on focus
